<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Upload</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            height: 100vh;
            margin: 0;
            padding: 0;
            font-size: 20px;
        }
        main {
            margin: 0 auto;
            width: 700px;
            text-align: center;
            align-self: center;
        }
        input {
            font-size: 20px;
        }
        button {
            font-size: 20px;
        }
        label {
            font-size: 20px;
        }
        textarea {
            font-size: 14px;
        }
        #message {
            font-size: 20px;
        }
        @media (width < 800px) {
            body {
            font-family: Arial, sans-serif;
            display: flex;
            height: 100vh;
            margin: 0;
            padding: 0;
            font-size: 14px;
        }
        main {
            margin: 0 auto;
            width: 400px;
            text-align: center;
            align-self: center;
        }
        input {
            font-size: 14px;
        }
        button {
            font-size: 14px;
        }
        label {
            font-size: 14px;
        }
        textarea {
            font-size: 12px;
            width: 380px;
            columns: 40;
        }
        #message {
            font-size: 14px;
        }
        }
    </style>
</head>
<body>
    <main>
        <h1>Test ml work</h1>
        <h1>2-5 minutes wait</h1>
        <input type="file" id="fileInput">
        <button onclick="submitFile()">Сгенерировать аннотацию</button>
        <br><br>
        <div id="message"></div>
        <br>
        <form id="responseForm">
            <label for="response">Response:</label><br>
            <textarea id="response" name="response" rows="6" cols="80"></textarea>
        </form>
    </main>
   

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="script.js"></script>
</body>
</html>
